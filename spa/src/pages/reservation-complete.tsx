// a simple front-end page that tells the user that reservation is complete.
// it will contain the user's full name, the room that was booked, the amount paid and the user's id number
export default function ReservationComplete() {
    return <h1>Reservation Complete</h1>;
  }